// Strings are placeholder until backend is ready

export const getSpacesEndpoint = "/spaces"

export const getSingleSpaceEndpoint = (id) => `/spaces/${id}`

export const getVideosFromSpaceEndpoint = (id) => `/spaces/${id}/videos`

export const getSingleVideoEndpoint = (id) => `/videos/${id}`